server:
  port: 8080

spring:
  application:
    name: captcha-backend

captcha:
  # 验证容差（像素）
  slider-tolerance: 8
  # 最短验证耗时（毫秒），用于拦截纯脚本快速提交
  min-elapsed-ms: 500
  click-tolerance: 18
  click-min-elapsed-ms: 800
  # 验证码缓存有效时间（秒）
  expire-seconds: 300
